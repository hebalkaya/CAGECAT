TODO: bug: downstream recompute module if shown as a downstream module of a recompute job,. (recompute -> recompute), which will lead to an error.
TODO: the Thornton backup folder should not be owned by user belt017 (instead should belong to Marnix, as discussed with Harm)
