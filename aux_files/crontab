# this file should be manually edited using the command:
# crontab -e
# as root when creating the container for the first time

PATH=/usr/local/bin

# minute hour day_of_month month day_of_week command
5 0 * * * cd /repo && python maint_remove_old_jobs.py
10 0 */3 * * bash /repo/maint_backup.sh